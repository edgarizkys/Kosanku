// KosanKu Pro Data Models & Database Schemas

// Prisma Schema Definition (schema.prisma)
/*
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum Role {
  ADMIN
  TENANT
}

enum RoomStatus {
  AVAILABLE
  OCCUPIED
  MAINTENANCE
}

enum PaymentStatus {
  PENDING
  SETTLED
  EXPIRED
  CANCELLED
  FAILED
}

enum ComplaintStatus {
  OPEN
  IN_PROGRESS
  RESOLVED
}

enum Channel {
  WHATSAPP
  EMAIL
  IN_APP
}

model User {
  id            String         @id @default(uuid())
  name          String
  email         String         @unique
  phone         String
  passwordHash  String
  role          Role           @default(TENANT)
  avatar        String?
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt

  rooms         Room[]         @relation("RoomTenant")
  invoices      Invoice[]
  complaints    Complaint[]
  notifications NotificationLog[]
}

model Room {
  id          String     @id @default(uuid())
  number      String     @unique
  type        String     // e.g. Single Deluxe, VIP Studio
  price       Float
  status      RoomStatus @default(AVAILABLE)
  floor       Int        @default(1)
  facilities  String[]   // AC, WiFi, Bathroom, TV
  tenantId    String?
  tenant      User?      @relation("RoomTenant", fields: [tenantId], references: [id])
  invoices    Invoice[]
  complaints  Complaint[]
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
}

model Invoice {
  id             String        @id @default(uuid())
  invoiceNumber  String        @unique
  userId         String
  user           User          @relation(fields: [userId], references: [id])
  roomId         String
  room           Room          @relation(fields: [roomId], references: [id])
  amount         Float
  penaltyAmount  Float         @default(0)
  totalAmount    Float
  dueDate        DateTime
  paymentStatus  PaymentStatus @default(PENDING)
  orderId        String?       @unique // Midtrans Order ID
  snapToken      String?
  paymentType    String?       // bank_transfer, qris, gopay, credit_card
  vaNumber       String?
  settledAt      DateTime?
  paymentLogs    PaymentLog[]
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
}

model PaymentLog {
  id             String   @id @default(uuid())
  invoiceId      String
  invoice        Invoice  @relation(fields: [invoiceId], references: [id])
  rawPayload     Json
  transactionStatus String
  statusCode     String
  createdAt      DateTime @default(now())
}

model Complaint {
  id          String          @id @default(uuid())
  userId      String
  user        User            @relation(fields: [userId], references: [id])
  roomId      String
  room        Room            @relation(fields: [roomId], references: [id])
  title       String
  description String
  category    String          // Plumbing, Electrical, Furniture, Internet
  imageUrl    String?
  status      ComplaintStatus @default(OPEN)
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt
}

model NotificationLog {
  id          String   @id @default(uuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  title       String
  message     String
  channel     Channel  @default(IN_APP)
  sentAt      DateTime @default(now())
  isRead      Boolean  @default(false)
}
*/
